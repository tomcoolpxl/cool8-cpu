# 04a. The I/O registers

**Generated by `tools/ioregs.py` from the Verilog that decodes them — do not edit.** `poe build` regenerates it and `poe check` fails if it is stale, if a peripheral has gained a register without a name, or if anything in `sw/` names an address no hardware answers at.

`$FE00-$FEFF` is decoded on the bus ahead of memory, so these addresses are never RAM. Each peripheral's Verilog gives its offsets as localparams whose value *is* the page offset — `A_IDX = 8'h50` is `$FE50` — so the addresses here are read from the hardware rather than restated.

What the registers *do* at the bit level is [04-system.md](04-system.md), written by hand.

## `cool8_flash.v`

| | | |
|---|---|---|
| `$FE88` | `FLS_ADDR_L` | SPI flash address, low |
| `$FE89` | `FLS_ADDR_M` | SPI flash address, middle |
| `$FE8A` | `FLS_ADDR_H` | SPI flash address, high |
| `$FE8B` | `FLS_DATA` | read the byte at that address |
| `$FE8C` | `FLS_CTRL` | start a read, select the device |
| `$FE8D` | `FLS_STAT` | busy and done |
| `$FE8E` | `FLS_WDATA` | byte to program |
| `$FE8F` | `FLS_WCTRL` | program and erase control |

## `cool8_pixport.v`

| | | |
|---|---|---|
| `$FE34` | `PIX_X_L` *(also `GPIXXL`)* | plot X, low |
| `$FE35` | `PIX_X_H` *(also `GPIXXH`)* | plot X, high |
| `$FE36` | `PIX_Y_L` *(also `GPIXYL`)* | plot Y, low |
| `$FE37` | `PIX_Y_H` *(also `GPIXYH`)* | plot Y, high |
| `$FE38` | `PIX_DATA` *(also `GPIXD`)* | write plots the pixel at X,Y |

## `cool8_ps2.v`

| | | |
|---|---|---|
| `$FE40` | `KBD_STAT` | PS/2 status: byte ready, error |
| `$FE41` | `KBD_DATA` | the scancode; reading clears ready |
| `$FE42` | `KBD_CTRL` — | interrupt enable and reset |
| `$FE43` | `KBD_TX` — | byte to send to the keyboard |
| `$FE44` | `KBD_MOD` — | modifier state, decoded in hardware |

## `cool8_snd.v`

| | | |
|---|---|---|
| `$FE50` | `SND_IDX` *(also `GSNDI`)* | voice to address |
| `$FE51` | `SND_DATA` *(also `GSNDD`)* | that voice's registers |

## `cool8_soc.v`

| | | |
|---|---|---|
| `$FE00` | `SYS_CTRL` — | system control: reset and bank selects |
| `$FE02` | `SYS_STAT` — | system status, read-only |
| `$FE03` | `LED` | the board's LEDs, one per bit |
| `$FE70` | `UART_STAT` | transmit ready, receive ready |
| `$FE71` | `UART_DATA` | read receives, write transmits |
| `$FE72` | `UART_DIV_L` — | baud divisor, low |
| `$FE73` | `UART_DIV_H` — | baud divisor, high |
| `$FE80` | `LDR_CTRL` — | boot loader control |
| `$FE81` | `LDR_STAT` — | boot loader status |

## `cool8_sprite.v`

| | | |
|---|---|---|
| `$FE2A` | `SPR_IDX` *(also `GSPRI`)* | sprite to address |
| `$FE2B` | `SPR_DATA` *(also `GSPRD`)* | that sprite's attribute bytes |
| `$FE2C` | `SPR_CTRL` *(also `GSPREN`)* | per-sprite enable bits |

## `cool8_vport.v`

| | | |
|---|---|---|
| `$FE26` | `VRAM_ADDR_L` *(also `GVRAL`)* | indirect VRAM address, low |
| `$FE27` | `VRAM_ADDR_H` *(also `GVRAH`)* | indirect VRAM address, high |
| `$FE28` | `VRAM_STEP` *(also `GVRST`)* | added to the address after each access |
| `$FE29` | `VRAM_DATA` *(also `GVRD`)* | the byte at that address; access auto-steps |

## `cool8_vregs.v`

| | | |
|---|---|---|
| `$FE10` | `VID_MODE` *(also `GVMODE`)* | screen mode, low nibble |
| `$FE11` | `VID_CTRL` — | display enable, sprite enable, blanking |
| `$FE12` | `VID_BASE_L` | character/bitmap base, low |
| `$FE13` | `VID_BASE_H` | character/bitmap base, high |
| `$FE14` | `VID_STRIDE_L` — | bytes per row, low |
| `$FE15` | `VID_STRIDE_H` — | bytes per row, high |
| `$FE16` | `VID_SCX_L` *(also `GSCRX`)* | horizontal scroll, low |
| `$FE17` | `VID_SCX_H` — | horizontal scroll, high |
| `$FE18` | `VID_SCY_L` — | vertical scroll, low |
| `$FE19` | `VID_SCY_H` — | vertical scroll, high |
| `$FE1A` | `VID_BORDER` — | border colour index |
| `$FE1B` | `VID_RASTER` — | current raster line, read-only |
| `$FE1C` | `VID_RCMP` — | raster line to compare against |
| `$FE1D` | `VID_IRQ` | interrupt enable and acknowledge |
| `$FE1E` | `PAL_IDX` *(also `GPALI`)* | palette entry to address |
| `$FE1F` | `PAL_DATA` *(also `GPALD`)* | that entry's colour |
| `$FE20` | `VID_PAT_L` | pattern/tile base, low |
| `$FE21` | `VID_PAT_H` | pattern/tile base, high |
| `$FE22` | `CUR_X` | text cursor column |
| `$FE23` | `CUR_Y` | text cursor row |
| `$FE24` | `CUR_CTRL` | cursor enable and blink |
| `$FE25` | `CUR_LINES` | first and last scanline of the cursor cell |

## Registers no software name reaches

18 of 58. The hardware decodes them and nothing in `sw/` has an equate for one, so reaching them means a bare `POKE $FExx`. Not a fault — several are read-only status or set once by the boot ROM — but the list is generated so it is a decision rather than an oversight.

`$FE00` SYS_CTRL, `$FE02` SYS_STAT, `$FE11` VID_CTRL, `$FE14` VID_STRIDE_L, `$FE15` VID_STRIDE_H, `$FE17` VID_SCX_H, `$FE18` VID_SCY_L, `$FE19` VID_SCY_H, `$FE1A` VID_BORDER, `$FE1B` VID_RASTER, `$FE1C` VID_RCMP, `$FE42` KBD_CTRL, `$FE43` KBD_TX, `$FE44` KBD_MOD, `$FE72` UART_DIV_L, `$FE73` UART_DIV_H, `$FE80` LDR_CTRL, `$FE81` LDR_STAT.

## Registers with more than one software name

12 of them, and all the same split: the interpreter uses its own `G`-prefixed shorthand while the boot ROM, the demo and the library use the longer name. Two spellings of one address is untidy but harmless, so the check reports rather than refuses; a spelling pointing at the *wrong* address is what it fails on.

| | |
|---|---|
| `$FE10` | `GVMODE` · `VID_MODE` |
| `$FE1E` | `GPALI` · `PAL_IDX` |
| `$FE1F` | `GPALD` · `PAL_DATA` |
| `$FE26` | `GVRAL` · `VRAM_ADDR_L` |
| `$FE27` | `GVRAH` · `VRAM_ADDR_H` |
| `$FE28` | `GVRST` · `VRAM_STEP` |
| `$FE29` | `GVRD` · `VRAM_DATA` |
| `$FE2A` | `GSPRI` · `SPR_IDX` |
| `$FE2B` | `GSPRD` · `SPR_DATA` |
| `$FE2C` | `GSPREN` · `SPR_CTRL` |
| `$FE50` | `GSNDI` · `SND_IDX` |
| `$FE51` | `GSNDD` · `SND_DATA` |
